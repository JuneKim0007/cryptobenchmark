# <setup>

Placeholder.

User config and user-supplied inputs for a run. Never contributes to a measurement.

Source: `android/app/src/main/java/com/example/cryptobenchmark/environment/setup/`

| Class | Does |
|---|---|
| `config/Config` | reads run parameters from `/sdcard/CryptoBenchmark.config` |

Device capability is not setup — it lives in `environment/discovery/`, see `probe.md` there.
