package io.github.junekim0007.cryptobench.benchmark.preparation.measurement

import java.util.Locale

internal object EngineTypeName {

    fun fold(type: String): String = type.uppercase(Locale.ROOT)
}
